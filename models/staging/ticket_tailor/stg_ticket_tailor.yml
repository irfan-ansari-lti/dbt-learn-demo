version: 2

models:
  - name: stg_ticket_tailor__orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      
      - name: currency_code
        tests:
          - accepted_values:
              values: ['USD']